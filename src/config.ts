/**
 * Site-wide constants. Everything a future update to the release process
 * would touch lives here — swap these instead of hunting through components.
 */

/** GitHub Releases "latest" always resolves to whatever tag is newest, so
 *  this URL never needs to change when a new APK ships. */
export const APK_DOWNLOAD_URL = "https://github.com/Pullolo/meowstrike/releases/latest";

/** Direct link to the releases list (for players who want to see changelogs
 *  or grab an older build). */
export const RELEASES_URL = "https://github.com/Pullolo/meowstrike/releases";

export const SITE_TITLE = "Meow Strike";
export const SITE_TAGLINE = "Zbierz drużynę kotów. Ruszcie razem w kosmos.";
